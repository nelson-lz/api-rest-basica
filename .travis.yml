language: java
branches:
  - develop
  - /^release-[0-999].[0-999]$/
  - master
notifications:
  email:
    recipients:
      - nelsonlz@outlook.com
# services:
# - mysql
# - mongodb
# before_install:
# - mysql -e 'CREATE DATABASE IF NOT EXISTS forge;'
script:
  #Test unitarios y de integracion con cobertura (jacoco)
  - mvn org.jacoco:jacoco-maven-plugin:prepare-agent verify
  #Sonarcloud
  - mvn sonar:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.organization=nelson-lz -Dsonar.login=$SONAR
  #Deploy https://api-rest-basica.herokuapp.com/system/version
deploy:
  provider: heroku
  api_key:
    secure: $HEROKU
  on:
    branch: master
  app: api-rest-basica #Por defecto este dato se toma del artifact pom